<!DOCTYPE html>
<html lang="en">

<head>
    {{>header}}
    <style>
        .content{
            min-height: 80vh
        }
        .empty {
            min-height: 80vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .empty>a>button {
            font-size: 1.5rem;
            background-color: #212a2f;
            margin-top: 2rem;
        }
    </style>
</head>

<body>

    {{>navbar}}
    <div class="content">
        {{#if products.length}}

        {{else}}
        <div class="empty">
            <h2 style="color: rgb(86, 79, 79); font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;">Seems like your cart is empty!!!!!!</h2>
            <a href="/"><button class="btn btn-success">Go To Home Page</button></a>
        </div>
        {{/if}}
        {{#each products}}
        {{>cartPartial this}}
        {{/each}}
    </div>
    {{>footer}}
</body>

</html>