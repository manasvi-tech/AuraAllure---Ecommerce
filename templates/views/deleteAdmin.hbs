<!DOCTYPE html>
<html lang="en">

<head>
    {{>header}}
    <link rel="stylesheet" href="css/style.css">

    <style>
        .product-delete-container {
            display: flex;
        }

        .product-delete-image,
        .product-delete-details {
            flex: 1;
            max-height: 70vh;
        }

        img {
            max-height: 70vh
        }

        .product-delete-details {
            padding-top: 5rem
        }

        .btn {
            margin-top: 1.3rem;
        }
    </style>
    
</head>

<body>
    {{>navbar}}
    <div class="product-delete-container">
        <div class="product-delete-image">
            <img src="{{product.imageUrl}}" alt="Image Unavailable">
        </div>
        <div class="product-delete-details">
            <h2 class="product-info-heading">{{product.name}}</h2>
            <h3 class="product-info-price">Rs. {{product.price}}</h3>
            {{product.id}}

            <div class="confirmation" style="margin-top:2rem">
                <h2 style="color:red">Are you sure you want to delete this item permanently?</h2>
                <form action="/deleteItem/{{product._id}}?_method=DELETE" method="POST"><button class="btn btn-danger" id="yes" style="font-size:1.3rem">Yes</button></form>
                <button class="btn btn-danger" id="no" style="font-size:1.3rem">No, Go Back</button>
            </div>
        </div>
    </div>

    {{>footer}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous">
        </script>

   

</body>

</html>